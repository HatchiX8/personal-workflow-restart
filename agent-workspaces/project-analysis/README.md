# Project Analysis

Project Analyst 未收到指定輸出位置時，會在本目錄建立或更新 `PROJECT_ANALYSIS.md`。

此文件可納入版本控制；若要讓 Workflow 自動載入，仍須在 `project.config.json` 的
`project_contexts` 中登錄相對路徑 `agent-workspaces/project-analysis/PROJECT_ANALYSIS.md`。

# Review Skills

此目錄保存 Target 或專業領域檢查 Skill。目前包含前端與後端檢查。

新增 Security、Performance、Database 或其他檢查時，應建立獨立 Skill Package，不修改 Review
固定 Workflow。共通且每次都必須執行的檢查才可放在 `checks/common.md`。

本 README 不參與 routing。

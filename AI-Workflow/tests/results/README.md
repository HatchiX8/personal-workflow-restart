# 驗收報告目錄

此目錄只存放本機執行產生的 Workflow 驗收報告，不存放測試程式、驗收情境、fixtures 或 expected results。

報告檔名使用：

```text
YYYYMMDD-HHmm-<驗收主題>.md
```

此目錄中的驗收報告預設由根目錄 `.gitignore` 排除；只有本說明檔納入版本控制。若特定報告需要保留作為版本稽核證據，應經團隊確認後再使用 Git 強制加入。
